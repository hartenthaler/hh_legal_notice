<?php

use Fisharebest\Webtrees\I18N;

/**
 * @var object $chapter
 */
?>

    <h3 class="hh-legal-notice-section"><?= e($chapter->getHeading()) ?></h3>

    <p>
        <?= I18N::translate('webtrees can check whether a newer webtrees version is available.') ?>
        <?= I18N::translate('For this purpose, the webtrees server sends a request to %s.', '<code dir="ltr">https://dev.webtrees.net/build/latest-version.txt</code>') ?>
        <?= I18N::translate('The request contains the installed webtrees version, the PHP version, a site identifier generated by webtrees, and the database driver used.') ?>
        <?= I18N::translate('The server receiving the request may also process normal technical request metadata, such as the IP address of this web server, the time of the request, and HTTP headers.') ?>
        <?= I18N::translate('webtrees stores the result locally and normally performs this check at most once per day; an administrator can also start a manual check in the control panel.') ?>
        <?= I18N::translate('The check is performed by the webtrees server, not by the visitor browser.') ?>
    </p>

    <p>
        <?= I18N::translate('Custom modules can provide their own latest-version URL.') ?>
        <?= I18N::translate('When the webtrees control panel is opened, webtrees may request these URLs to compare the installed module version with the latest version offered by the module author.') ?>
        <?= I18N::translate('This request may transmit the IP address of this web server and normal technical request metadata to GitHub.') ?>
        <?= I18N::translate('The result is cached locally for up to one day.') ?>
    </p>
