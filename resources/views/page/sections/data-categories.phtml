<?php

use Fisharebest\Webtrees\I18N;
?>

    <h3><?= e($chapter->getHeading()) ?></h3>

    <h4><?= I18N::translate('Personal data about ancestors, family members, and other persons') ?></h4>

    <p>
        <?= I18N::translate('Personal data about living persons is also stored for the purpose of family history and genealogical research; see inventory data and content data above.') ?>
        <?php if ($useEuPrivacyLaw) : ?>
            <?= I18N::translate('Family history and genealogical research is a legitimate interest under Article 6(1), first sentence, point (f) of the General Data Protection Regulation (EU GDPR).') ?>
        <?php else : ?>
            <?= I18N::translate('Family history and genealogical research is the legitimate purpose for processing this data.') ?>
        <?php endif ?>
        <?= I18N::translate('Personal data that you find on this website, such as names, e-mail addresses, photos, addresses, or life dates, is used only for family history and genealogical research and is kept securely.') ?>
        <?= I18N::translate('It is stored indefinitely because it has historical value for genealogy.') ?>
    </p>

    <?php if ($useGermanPrivacyLaw) : ?>
        <p>
            <?= I18N::translate('In addition to the General Data Protection Regulation (EU GDPR), German national data protection rules may apply, in particular the Federal Data Protection Act (BDSG).') ?>
        </p>
    <?php endif ?>

    <?php if ($useEuPrivacyLaw) : ?>
        <h4><?= I18N::translate('Legal bases for processing') ?></h4>

        <p>
            <?= I18N::translate('The legal bases for processing personal data depend on the respective processing operation.') ?>
            <?= I18N::translate('The provision and secure operation of this website, the protection of living persons, and family history research are based on legitimate interests under Article 6(1), first sentence, point (f) GDPR.') ?>
            <?= I18N::translate('Processing related to user accounts, registered access, and communication with registered users may also be based on contractual or pre-contractual relationships under Article 6(1), first sentence, point (b) GDPR.') ?>
            <?= I18N::translate('If consent is requested for individual processing operations, the legal basis is Article 6(1), first sentence, point (a) GDPR.') ?>
            <?= I18N::translate('Where legal obligations require processing or retention, the legal basis is Article 6(1), first sentence, point (c) GDPR.') ?>
        </p>
    <?php endif ?>

    <h4><?= I18N::translate('Graduated access rights to personal data') ?></h4>

    <p>
        <?= I18N::translate('Large parts of the content of this website are freely accessible.') ?>
        <?= I18N::translate('This does not apply to data about persons who are still living.') ?>
        <?= I18N::translate('Only registered users who have been assigned graduated rights have access to this data.') ?>
        <?= I18N::translate('Additional data is stored about these users, such as their name and e-mail address.') ?>
        <?= I18N::plural('Registered users are only persons related to me by blood or marriage.', 'Registered users are only persons related to us by blood or marriage.', $singular ? 1 : 2) ?>
        <?= I18N::translate('A substantial part of the content here has been shared confidentially within the family.') ?>
        <?= I18N::translate('It must remain confidential; therefore we all depend on every individual treating this data confidentially.') ?>
    </p>

    <h4><?= I18N::translate('Personal data about users with a user account') ?></h4>

    <p>
        <?= I18N::translate('The personal data that you provide yourself when creating an account is stored and processed; see contact data.') ?>
    </p>

    <ul>
        <li><?= I18N::translate('Name') ?></li>
        <li><?= I18N::translate('E-mail address') ?></li>
        <li><?= I18N::translate('Password') ?></li>
    </ul>

    <p>
        <?= I18N::translate('Other personal data that you actively provide is also stored and used.') ?>
        <?= I18N::translate('This includes, for example, data created by completing your personal details on your personal page or by creating family relationships.') ?>
    </p>

    <p>
        <?= I18N::translate('Furthermore, data relating to your user account is recorded as a result of your activities on our website, for example when editing records.') ?>
    </p>

    <p>
        <?= I18N::plural('I occasionally use your e-mail address to inform you by e-mail about important matters relating to the website or, if necessary, to contact you with questions about changes you have made.', 'We occasionally use your e-mail address to inform you by e-mail about important matters relating to the website or, if necessary, to contact you with questions about changes you have made.', $singular ? 1 : 2) ?>
    </p>

    <p>
        <?= I18N::translate('If you have your user account disabled here, the genealogical data you entered will remain available for the benefit of our genealogical research.') ?>
        <?= I18N::translate('The data about your person will also be retained; only your access to the specially protected areas here will be blocked.') ?>
    </p>

    <h4><?= I18N::translate('Personal data about visitors to this website') ?></h4>

    <p><?= I18N::translate('In principle, data that can be related to non-logged-in users of this service, visitors, is also stored; see usage data and communication data.') ?></p>

    <h4><?= I18N::translate('Security measures') ?></h4>

    <p>
        <?= I18N::translate('Appropriate technical and organizational measures are taken in accordance with legal requirements, taking into account the state of the art, implementation costs, the nature, scope, circumstances, and purposes of processing, and the varying likelihood and severity of the risk to the rights and freedoms of natural persons, in order to ensure a level of security appropriate to the risk.') ?>
    </p>

    <p>
        <?= I18N::translate('These measures include, in particular, securing the confidentiality, integrity, and availability of data, including by controlling access to the data.') ?>
    </p>

    <h4><?= I18N::translate('General risk') ?></h4>

    <p>
        <?= I18N::plural('I point out that data transmission on the Internet, for example when communicating by e-mail, may have security gaps.', 'We point out that data transmission on the Internet, for example when communicating by e-mail, may have security gaps.', $singular ? 1 : 2) ?>
        <?= I18N::translate('Complete protection of data against access by third parties is also not possible.') ?>
    </p>

    <h4><?= I18N::translate('Cookies') ?></h4>

    <p>
        <?= I18N::translate('This website uses cookies to enable user logins and to store settings such as your selected language.') ?>
        <?= I18N::translate('Cookies help make our service more user-friendly, effective, and secure.') ?>
        <?= I18N::translate('Cookies are small text files that are stored on your computer and saved by your browser.') ?>
        <?= I18N::translate('These cookies are strictly necessary for the website to function properly.') ?>
        <?= I18N::translate('Session cookies are deleted when you leave the website or close your browser.') ?>
        <?= I18N::translate('Persistent cookies can remain stored after the browser is closed, for example to preserve selected settings for a later visit.') ?>
        <?= I18N::translate('You can configure your browser so that you are informed when cookies are set, allow cookies only in individual cases, exclude the acceptance of cookies in certain cases or in general, and enable automatic deletion of cookies when closing the browser.') ?>
        <?= I18N::translate('If cookies are disabled, the functionality of this website may be restricted.') ?>
    </p>

    <p>
        <?php if (count($cookiesServices) > 0) : ?>
            <?= I18N::translate('This website uses third-party services to manage cookies.') ?>
            <?php foreach ($cookiesServices as $service => $url) : ?>
                <li>
                    <?= $service ?> - <a href="<?= e($url) ?>"><?= e($url) ?></a>
                </li>
            <?php endforeach ?>
        <?php endif ?>
    </p>

    <?php if ($analytics->isNotEmpty() || count($trackingServices) > 0) : ?>
        <h4><?= I18N::translate('Tracking and analytics') ?></h4>
        <p>
            <ul>
                <li>
                    <?= I18N::translate('This website uses third-party services to learn about visitor behavior.') ?>
                    <ul>
                        <?php if ($analytics->isNotEmpty()) : ?>
                            <?php foreach ($analytics as $module) : ?>
                                <li>
                                    <?= $module->title() ?> -
                                    <a href="<?= e($module->externalUrl()) ?>"><?= e($module->externalUrl()) ?></a>
                                </li>
                            <?php endforeach ?>
                        <?php endif ?>
                        <?php if (count($trackingServices) > 0) : ?>
                            <?php foreach ($trackingServices as $service => $url) : ?>
                                <li>
                                    <?= $service ?> - <a href="<?= e($url) ?>"><?= e($url) ?></a>
                                </li>
                            <?php endforeach ?>
                        <?php endif ?>
                    </ul>
                </li>

                <li>
                    <?= I18N::translate('These services may use cookies or other tracking technology.') ?>
                </li>

                <li>
                    <?= I18N::translate('You can opt out of tracking by setting the "Do Not Track" header in your browser preferences.') ?>
                </li>
            </ul>
        </p>
    <?php endif ?>

    <?php if (count($thirdPartyServices) > 0 || count($externalTranscriptionProviders) > 0) : ?>
        <h4><?= I18N::translate('Third party services') ?></h4>

        <p>
            <?= I18N::translate('When external services are used, personal data may be transferred to or disclosed to other providers, companies, or persons where this is necessary for the respective service.') ?>
            <?= I18N::translate('Only those services that are configured or actually used by this website are listed here.') ?>
        </p>

        <?php if (count($thirdPartyServices) > 0) : ?>
            <p><?= I18N::translate('This website uses additional third-party services.') ?></p>

            <table class="table table-sm">
                <thead>
                    <tr>
                        <th><?= I18N::translate('Service') ?></th>
                        <th><?= I18N::translate('URL') ?></th>
                        <th><?= I18N::translate('Location') ?></th>
                    </tr>
                </thead>
                <tbody>
                <?php foreach ($thirdPartyServices as $service) : ?>
                    <tr>
                        <td><strong><?= e($service['name']) ?></strong></td>
                        <td><a href="<?= e($service['url']) ?>"><?= e($service['url']) ?></a></td>
                        <td><?= $service['country'] !== '' ? e($service['country']) : I18N::translate('not specified') ?></td>
                    </tr>
                <?php endforeach ?>
                </tbody>
            </table>

            <?php $thirdPartyServicesWithData = array_filter($thirdPartyServices, static fn (array $service): bool => count($service['data'] ?? []) > 0 || ($service['privacy_url'] ?? '') !== '') ?>
            <?php if (count($thirdPartyServicesWithData) > 0) : ?>
                <ul>
                    <?php foreach ($thirdPartyServicesWithData as $service) : ?>
                        <li>
                            <strong><?= e($service['name']) ?></strong>
                            <?php if (($service['name'] ?? '') === 'OpenStreetMap') : ?>
                                <?= I18N::translate('This website may embed maps from OpenStreetMap. The map content is loaded from OpenStreetMap servers, and OpenStreetMap may process data such as IP addresses, displayed location data, and selected map settings to provide and cache the map functions.') ?>
                            <?php endif ?>
                            <?php if (count($service['data'] ?? []) > 0) : ?>
                                <br>
                                <?= I18N::translate('Data that may be processed by this service:') ?>
                                <ul>
                                    <?php foreach ($service['data'] as $dataCategory) : ?>
                                        <?php $dataCategoryLabel = match ($dataCategory) {
                                            'IP addresses' => I18N::translate('IP addresses'),
                                            'Location data shown on maps' => I18N::translate('Location data shown on maps'),
                                            'Map display settings' => I18N::translate('Map display settings'),
                                            default => I18N::translate($dataCategory),
                                        } ?>
                                        <li><?= e($dataCategoryLabel) ?></li>
                                    <?php endforeach ?>
                                </ul>
                            <?php endif ?>
                            <?php if (($service['privacy_url'] ?? '') !== '') : ?>
                                <?= I18N::translate('Privacy notice:') ?>
                                <a href="<?= e($service['privacy_url']) ?>"><?= e($service['privacy_url']) ?></a>
                            <?php endif ?>
                        </li>
                    <?php endforeach ?>
                </ul>
            <?php endif ?>
        <?php endif ?>

        <?php if (count($externalTranscriptionProviders) > 0) : ?>
            <h5><?= I18N::translate('External transcription providers') ?></h5>

            <p>
                <?= I18N::translate('This website may use external transcription providers when a registered user explicitly starts such a workflow.') ?>
                <?= I18N::translate('In that case, the following provider services and data categories may be involved:') ?>
            </p>

            <ul>
                <?php foreach ($externalTranscriptionProviders as $provider) : ?>
                    <li>
                        <strong><?= e($provider['name'] ?? '') ?></strong>
                        <?php if (($provider['country'] ?? '') !== '') : ?>
                            (<?= I18N::translate('Location: %s', e($provider['country'])) ?>)
                        <?php endif ?>
                        <?php if (($provider['url'] ?? '') !== '') : ?>
                            -
                            <a href="<?= e($provider['url']) ?>"><?= e($provider['url']) ?></a>
                        <?php endif ?>

                        <?php if (count($provider['data'] ?? []) > 0) : ?>
                            <br>
                            <?= I18N::translate('Data that may be exchanged:') ?>
                            <ul>
                                <?php foreach ($provider['data'] as $dataCategory) : ?>
                                    <li><?= e($dataCategory) ?></li>
                                <?php endforeach ?>
                            </ul>
                        <?php endif ?>
                    </li>
                <?php endforeach ?>
            </ul>
        <?php endif ?>

        <?php $thirdCountryTransferServices = array_filter([...$thirdPartyServices, ...$externalTranscriptionProviders], static fn (array $service): bool => (bool) ($service['thirdCountryTransfer'] ?? false)) ?>
        <?php if ($useEuPrivacyLaw && count($thirdCountryTransferServices) > 0) : ?>
            <h5><?= I18N::translate('Data transfer to third countries') ?></h5>

            <p>
                <?= I18N::translate('Some third-party services are provided from countries outside the European Union.') ?>
                <?= I18N::translate('If personal data is transferred to such third countries, this is done only in accordance with the legal requirements of Articles 44 to 49 GDPR, for example on the basis of an adequacy decision, standard contractual clauses, or another legally permitted safeguard.') ?>
            </p>

            <p>
                <?= I18N::translate('This may apply to the following services:') ?>
                <?php foreach (array_values($thirdCountryTransferServices) as $index => $service) : ?>
                    <?= $index > 0 ? ', ' : '' ?><strong><?= e($service['name'] ?? '') ?></strong>
                <?php endforeach ?>.
            </p>
        <?php endif ?>
    <?php endif ?>

    <h4><?= I18N::translate('Hosting for the webtrees server') ?></h4>

    <p>
        <?php if ($hostingCompanyName !== '') : ?>
            <?= I18N::translate('This website is operated by the hosting provider') ?>
            <?php if ($hostingCompanyUrl !== '') : ?>
                <a href="<?= e($hostingCompanyUrl) ?>"><?= e($hostingCompanyName) ?></a>
            <?php else : ?>
                <?= e($hostingCompanyName) ?>
            <?php endif ?>
            .
            <?php if ($hostingPrivacyNotice !== '' && $hostingCompanyName !== '') : ?>
                <?= I18N::translate('For details, please refer to the privacy notice of %s.', e($hostingCompanyName)) ?>
                <a href="<?= e($hostingPrivacyNotice) ?>"><?= e($hostingPrivacyNotice) ?></a>.
            <?php endif ?>
        <?php endif ?>
    </p>

    <p>
        <?= I18N::translate('The hosting provider automatically collects and stores information when this website is accessed, such as browser used, operating system, referring page, IP address, time of access, and similar data.') ?>
        <?= I18N::translate('This data cannot be assigned to specific persons without consulting additional data sources, and') ?>
        <?= I18N::plural('I do not evaluate this data further', 'we do not evaluate this data further', $singular ? 1 : 2) ?>
        <?= I18N::translate('unless there is unlawful use of these web pages.') ?>
    </p>

    <?php if ($orderProcessing) : ?>
        <h5><?= I18N::translate('Order processing') ?></h5>

        <p><?= I18N::translate('An agreement on order processing has been concluded with the hosting provider.') ?>
            <?php if ($hostingStartDate !== '' || $hostingEndDate !== '') : ?>
                <?= I18N::translate('This took place') ?>
                <?php if ($hostingStartDate !== '') : ?>
                    <?= I18N::translate('for the first time on %s', e($hostingStartDate)) ?>
                    <?php if ($hostingEndDate !== '') : ?>
                        <?= I18N::translate('and') ?>
                    <?php endif ?>
                <?php endif ?>
                <?php if ($hostingEndDate !== '') : ?>
                    <?= I18N::translate('most recently on %s', e($hostingEndDate)) ?>
                <?php endif ?>.
            <?php endif ?>
            <?php if ($useEuPrivacyLaw) : ?>
                <?= I18N::translate('This is an agreement required by data protection law under Article 28 GDPR, which ensures that the hosting provider processes the personal data of this website') ?>
            <?php else : ?>
                <?= I18N::translate('This agreement ensures that the hosting provider processes the personal data of this website') ?>
            <?php endif ?>
            <?= I18N::plural('only according to my instructions', 'only according to our instructions', $singular ? 1 : 2) ?>
            <?php if ($useEuPrivacyLaw) : ?>
                <?= I18N::translate('and in compliance with the GDPR.') ?>
            <?php else : ?>
                <?= I18N::translate('and in compliance with the applicable data protection requirements.') ?>
            <?php endif ?>
        </p>
    <?php endif ?>

    <h4><?= I18N::translate('Application log') ?></h4>

    <p>
        <?= I18N::translate('The application used (webtrees) logs all user actions so that, among other things, changes to the data can be traced and approved.') ?>
    </p>
